const express = ('express');
const app = ('');

const ejs = ('ejs');
const port = 2500;

app.set('view engine','ejs');
app.set('view',__dirname + '/view');
{
  "dependencies": {
    "ejs": "^3.1.9",
    "express": "^4.18.2",
    "fs": "^0.0.1-security"
  }
}

